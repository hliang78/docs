# 【接口文档】{系统名称}_{模块名称}_{版本号}

## 1. 文档信息

| 项目 | 内容 |
|------|------|
| 系统名称 | {系统名称} |
| 模块名称 | {模块名称} |
| 版本号 | {版本号} |
| 创建日期 | {yyyy-mm-dd} |
| 接口基准地址 | {如：http://localhost:8080/api/v1} |
| 通用响应格式 | {如：ResponseModel/Result<T>} |
| 关联文档 | 【需求概要】{系统名称}_{版本号}.md<br>【数据库设计】{系统名称}_{模块名称}_{版本号}.md |

## 2. 通用说明

### 2.1 通用请求头

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| Content-Type | string | 是 | application/json |
| Authorization | string | 是（需登录接口除外） | Bearer {token} |

### 2.2 通用响应结构

```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "timestamp": 1700000000000
}
```

### 2.3 状态码说明

| 状态码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 参数错误 |
| 401 | 未授权/未登录 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |
| 601 | 权限不足 |

## 3. 接口列表

### 3.1 {接口功能名称}

**接口描述**：{简要描述接口的业务功能}

**请求信息**

| 项目 | 内容 |
|------|------|
| 接口URL | {HTTP方法} {接口路径} |
| 请求方式 | GET/POST/PUT/DELETE |
| Content-Type | application/json |
| 权限要求 | {登录/角色/权限码/无} |

**请求参数**

| 参数名 | 类型 | 位置 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|------|
| id | Long | path | 是 | 资源ID | 1001 |
| pageNum | Integer | query | 否 | 页码，默认 1 | 1 |
| {参数名} | {类型} | body/query/path/header | {是/否} | {说明} | {示例} |

**请求示例**

```http
GET /api/v1/{resource}?pageNum=1&pageSize=10 HTTP/1.1
Host: localhost:8080
Authorization: Bearer {token}
Content-Type: application/json
```

**Body 示例**

```json
{
  "name": "示例名称",
  "status": "enable"
}
```

**响应示例（成功）**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1001,
    "name": "示例名称",
    "status": "enable",
    "createTime": "2026-05-15 10:00:00"
  },
  "timestamp": 1778800000000
}
```

**响应示例（失败）**

```json
{
  "code": 400,
  "message": "{错误信息}",
  "data": null,
  "timestamp": 1778800000000
}
```

**业务规则与校验**

| 规则 | 处理方式 |
|------|----------|
| {规则} | {校验/错误码/提示} |

## 4. 接口汇总表

| 序号 | 功能 | 请求方式 | URL | 说明 |
|------|------|----------|-----|------|
| 1 | {功能名称} | GET/POST | {URL} | {说明} |

## 5. 待确认事项

| 序号 | 待确认内容 | 影响接口 | 状态 |
|------|------------|----------|------|
| 1 | {问题} | {接口} | 待确认 |

