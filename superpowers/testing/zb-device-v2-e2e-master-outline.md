# ZB / Device V2 E2E Master Outline

配套文档：
- AI 交接：[zb-device-v2-e2e-ai-handoff.md](/home/jacky/project/OneOPS-ALL/docs/superpowers/testing/zb-device-v2-e2e-ai-handoff.md)
- 易忽略因素：[zb-device-v2-e2e-overlooked-factors.md](/home/jacky/project/OneOPS-ALL/docs/superpowers/testing/zb-device-v2-e2e-overlooked-factors.md)

注：`ZB-*`、`UII-*`、`UIS-*`、`CMP-*` 当前主要表示基于 sqlite + controllable DC2/manage/monitor collaborator 的 harnessed integration 结果；`REAL-*` 表示 2026-05-31 至 2026-06-01 在 `demo-test` 实例上完成的全真联调结果。`finding-real` 表示真实链路已跑通，但暴露了需要继续跟踪的数据正确性问题。

| Case ID | Entry | Device Type | Tag | One-line Description | Status |
| --- | --- | --- | --- | --- | --- |
| ZB-001 | ZB | 网络设备 | 正常 | ZB 录入一台 SNMP-only 网络设备，预期完成 seed、pipeline、detect、collect、store、manage 和 monitor push 成功。 | passed |
| ZB-002 | ZB | 服务器 | precheck | ZB 录入服务器但缺 SSH 密码，预期同步 precheck 拦截且不写 seed。 | passed |
| ZB-003 | ZB | 网络设备 | collect | ZB 录入网络设备后 detect 成功但 facts 缺 serial_number，预期在 store decision 被阻断且 monitor push 跳过。 | passed |
| ZB-004 | ZB | 网络设备 | agent | ZB 录入网络设备成功进入 manage 但 monitor push 结构化失败，预期 callback、task 与 entity 一致呈现 partial/failed monitor push。 | passed |
| ZB-005 | ZB | 复用设备 | seed | ZB 录入设备时命中多个已有 device_v2 复用候选，预期卡在 seed 的 resolve_existing_device 且不启动 pipeline。 | passed |
| ZB-006 | ZB | 网络设备 | store | ZB 录入网络设备后 facts 命中其他设备的身份锚冲突，预期 store decision 返回 identity conflict 且不进入 manage。 | passed |
| ZB-007 | ZB | 网络设备 | sync | ZB 录入网络设备后 manage 阶段 sync_to_v1 结构化失败，预期 callback、task 与 entity 一致呈现 failed/skipped。 | passed |
| ZB-008 | ZB | 复用设备 | detect | ZB 复用一台带旧画像的 device_v2 后，当前 detect/collect 返回新画像，预期平台、厂商、型号与主机名被纠偏到当前事实。 | passed |
| ZB-009 | ZB | 网络设备 | collect | ZB 录入网络设备后 detect 识别成需要 CLI 的服务器，但源数据只有 SNMP 凭据，预期在 collect/resolve_credentials 返回 D2_STORE_CREDENTIAL_MISSING。 | passed |
| ZB-010 | ZB | 网络设备 | callback | ZB 凭据缺失导致 collect 阻断时，预期 callback 顶层 success/code/message 与 summary_v2 行状态一致且不泄露内部细节。 | passed |
| ZB-011 | ZB | 混合批次 | callback | ZB 同一批次内混合成功、collect 阻断和 monitor push 失败设备时，预期 callback 顶层收敛为 ZB_STORE_PARTIAL_FAILED，且各 summary_v2 行与实体状态一致。 | passed |
| UII-001 | 页面导入 | 网络设备 | 正常 | 页面 base pass 导入网络设备并 apply 后启动 pipeline，预期写出 import record、task、store run 与 monitor push 成功。 | passed |
| UII-002 | 页面导入 | 网络设备 | validate | 页面导入重复 code 记录，预期 validate 标记错误且 apply 不写实体。 | passed |
| UII-003 | 页面导入 | 网络设备 | seed | 页面导入网络设备时 rack 与 site 主数据层级冲突，预期 apply 失败且不写实体。 | passed |
| UII-004 | 页面导入 | 网络设备 | 对照 | 页面导入网络设备并启动 pipeline 后，预期 import record、entity 与最终 device_v2 projection 在状态和关键 attrs 上保持一致。 | passed |
| UIS-001 | 页面store | 复用设备 | 正常 | 页面对已有 device_v2 触发 store/start，预期复用原实体并进入真实 detect/collect 链路。 | passed |
| UIS-002 | 页面store | 网络设备 | agent | 页面对网络设备触发 store/start 后 monitor push 返回结构化失败，预期 task 为 partial 且实体持久化 monitor_push_status=failed。 | passed |
| UIS-003 | 页面store | 网络设备 | sync | 页面对网络设备触发 store/start 后 sync_to_v1 创建失败，预期 task failed 且实体持久化 sync_to_v1_status=failed。 | passed |
| UIS-004 | 页面store | 网络设备 | agent | 页面对网络设备触发 store/start 成功后，预期 monitor push 下发包含正确的 credential source 映射。 | passed |
| UIS-005 | 页面store | 复用设备 | detect | 页面对带旧画像的 device_v2 触发 store/start，当前 detect/collect 返回新画像时，预期平台、厂商、型号与主机名被纠偏到当前事实。 | passed |
| UIS-006 | 页面store | 网络设备 | collect | 页面对设备触发 store/start 时 detect 识别成需要 CLI 的服务器，但实体只有 SNMP 凭据，预期在 collect/resolve_credentials 返回 D2_STORE_CREDENTIAL_MISSING。 | passed |
| UIS-007 | 页面store | 网络设备 | 对照 | 页面凭据缺失导致 collect 阻断时，预期 task、store run 与 entity 三层状态一致呈现 blocked/store/pending。 | passed |
| UIS-008 | 页面store | 网络设备 | 对照 | 页面凭据缺失导致 collect 阻断时，预期最终 device_v2 projection 与 entity 在 stage/manage/attrs 上保持一致。 | passed |
| CMP-001 | ZB vs 页面导入 | 网络设备 | 对照 | 同一台网络设备分别从 ZB 和页面导入进入，预期 detect、collect、store decision、DB 最终态和 monitor push 一致。 | passed |
| CMP-002 | ZB vs 页面store | 网络设备 | 对照 | 同一台网络设备分别从 ZB 和页面 store/start 进入，预期 task stage、store run 和 monitor push 行为一致。 | passed |
| CMP-003 | ZB vs 页面store | 网络设备 | 对照 | 同一类 facts 身份锚冲突分别从 ZB 和页面 store/start 进入，预期都在 store decision 返回 D2_STORE_IDENTITY_CONFLICT 且不触发 monitor push。 | passed |
| CMP-004 | ZB vs 页面store | 复用设备 | 对照 | 同一台带旧画像的复用设备分别从 ZB 和页面 store/start 进入，预期都会被当前 detect/collect 纠偏到一致的新画像。 | passed |
| CMP-005 | ZB vs 页面store | 网络设备 | 对照 | 同一台设备分别从 ZB 和页面 store/start 进入后都被 detect 识别成需要 CLI 的服务器，但只有 SNMP 凭据，预期两边都在 collect/resolve_credentials 返回 D2_STORE_CREDENTIAL_MISSING。 | passed |
| CMP-006 | ZB vs 页面导入 | 混合批次 | 对照 | ZB 混合批次与页面导入后批量 store/start 同时包含成功设备和 collect 阻断设备时，预期批次汇总与逐设备最终状态一致。 | passed |
| REAL-001 | 页面store | 网络设备 | real-success | 在 `demo-test` 实例上对 `DVCBAEFDC43A8F3` 触发真实 store/start，预期 task success、entity 落成 done/success、`sync_to_v1_status=skipped`、`monitor_push_status=success`，并写入 agent 任务表。 | passed-real |
| REAL-002 | 页面store | 网络设备 | real-detect | 在 `demo-test` 实例上对 `DVC8D84ACA72FB8` 触发真实 store/start，预期 controller detect 真失败并在 `D2_STORE_CONTROLLER_DETECT_FAILED` 阻断。 | passed-real |
| REAL-003 | 页面store | 网络设备 | real-conflict | 在 `demo-test` 实例上对 `DVCCBE9EB0CB74F` 触发真实 store/start，预期 detect/collect 真跑通后因已有 ZB 记录命中 `D2_STORE_IDENTITY_CONFLICT`。 | passed-real |
| REAL-004 | ZB | 网络设备 | real-seed | 在 `demo-test` 实例上临时启用 `system.extSys.zb` 并捕获真实 callback 后，对 `172.32.2.14` 发起 ZB 请求，预期同步受理成功、callback 真外发成功，并在 `seed/resolve_existing_device` 因 SN 多命中返回 `ZB_SEED_WRITE_FAILED`。 | passed-real |
| REAL-005 | 页面导入 | 服务器 | real-import-content | 在 `demo-test` 实例上对 `DVCBAEFDC43A8F3` 走真实 `CreateBatch -> Upload -> Validate -> Apply -> store/start`，预期 batch/record、entity、DC2 采集与 agent 任务内容一致；若导入时注入脏 `sn`，则主记录保留该值但必须落成 `sn_quality=seeded_unobserved`、`sn_identity_usable=false`，且不再作为可信 identity 锚。 | passed-real |
| REAL-006 | 页面store | 网络设备 | real-fix | 在修复 blocked store 的旧纳管投影泄漏后，重新回放 `DVCCBE9EB0CB74F` 的真实 identity conflict，预期 entity 清除陈旧 `sync_to_v1_status/monitor_push_status`，仅保留历史 `device_v1_code`。 | passed-real |
| REAL-007 | ZB | 复用设备 | real-finding | 在 `demo-test` 实例上对已真实成功过的 `DVC19218A412EA1` 连续发起 ZB 入库，发现命中现有 `device_v2` 时会把原本可用的 `credential_ref_in_band=ingest_cli_B02A796CC80D` 覆盖成新的 `SCT...` 引用，随后同设备的 ZB 与页面 `store/start` 都真实卡在 `collect -> D2_STORE_DATASET_FAILED`。 | finding-real |
| REAL-008 | ZB | 复用设备 | real-fix | 修复 ZB 复用已有 `device_v2` 时覆盖现有凭据引用的问题后，回放 `zb-real-20260601-0032`，预期 `credential_ref_in_band` 持续保持 `ingest_cli_B02A796CC80D`，链路不再死在 `collect`；本次真实结果继续推进到 `sync_to_v1/bind_credentials`，并暴露新的 `SYNC_V1_CREDENTIAL_BIND_FAILED`。 | passed-real |
| REAL-009 | 页面sync / manage | 复用设备 | real-recheck | 在同一套真实 DB/配置下对 `DVC19218A412EA1` 进程内直调 `RunSyncToV1` 复测，确认 `bind_credentials` 实际可通过 `SecretHub` 引用兜底，`monitor_push=false` 时全程 success，`monitor_push=true` 时失败点稳定落在 `monitor_push/apply_monitoring_strategy`；当前实体状态因此收敛为 `sync_to_v1_status=skipped`、`monitor_push_status=failed`。 | passed-real |
| REAL-010 | ZB | 复用设备 | real-fix | 给 ZB 入口构造的 `DeviceV2API` 补齐真实 `DB` 后，回放 `zb-real-20260601-0034`，确认 `bind_credentials` 不再误判缺失，链路真实走通到 `done/success + monitor_push=success`，同时暴露 callback 行消息仍误报“sync 失败缺错误码”。 | finding-real |
| REAL-011 | ZB | 复用设备 | real-success | 修复 `manage runtime` 将 `skipped + success` 的 `sync item` 错误标准化成失败消息后，回放 `zb-real-20260601-0035`，确认 ZB callback、DB、pipeline、agent 任务内容四边一致呈现成功，`snmp-passthrough` 目标保持 `udp://192.168.100.19:161`，`ping` 目标保持 `192.168.100.19`。 | passed-real |
| REAL-012 | 页面store | 复用设备 | real-success | 在 `demo-test` 实例上从页面 `store/start` 回放 `DVC19218A412EA1`，确认页面返回 `task_id=entv2_1780271190801693778` 后，DB、task summary 与 agent 配置内容和 ZB 成功链路一致，`sync_to_v1_status=skipped`、`monitor_push_status=success`，并保持 `ping/snmp` 目标内容正确。 | passed-real |
| REAL-013 | 页面导入 | 复用设备 | real-finding | 在 `demo-test` 实例上对 `DVCBAEFDC43A8F3` 发起真实 `pass_1_base` 导入并故意注入 `rack=HX-MM-404` 后，发现旧实现会让 `validate/apply` 直接通过，并把 `attributes.rack=HX-MM-404` 写进实体，同时保留旧 `location_node_code=RACK20251219000001`，形成真实位置字段不一致污染。 | finding-real |
| REAL-014 | 页面导入 | 复用设备 | real-recovery | 修复导入校验后，使用真实批次 `real-import-recovery-2-075826` 先以 `rack=HX-MM-404` 触发 `IMPORT_VALIDATE_ERROR` 且确认实体不变，再通过 `records/update -> validate -> apply -> store/start` 回放 `task_id=entv2_1780271906663265110`，最终使 import record、DB、页面投影与 agent 配置内容一致收敛到 `site_code=SITE20251209000001`、`rack_code=RACK20251219000001`、`monitor_push_status=success`，并再次验证 `ping=192.168.100.15`、`snmp=udp://192.168.100.15:161`。 | passed-real |
| REAL-015 | 页面导入 / 页面store | 复用设备 | real-blocked-recovery | 在 `demo-test` 实例上使用真实批次 `real-import-cred-block-080313` 将 `DVCBAEFDC43A8F3` 的 `credential_ref_in_band` 注入为不存在的 `missing-real-cli-080313` 后，`apply` 会真实写入脏凭据，但后续 `store/start` 在 `task_id=entv2_1780272193383911549` 稳定阻断于 `device_collection2 -> D2_STORE_DATASET_FAILED`，同时实体收敛为 `store/blocked/pending` 且 `sync_to_v1_status/monitor_push_status` 被清空、agent 侧 `ping/snmp` 任务时间戳保持不变；随后恢复原凭据并回放 `task_id=entv2_1780272195577113262`，再次真实收敛到 `done/success/success` 且重新刷新正确的 agent 配置内容。 | passed-real |
| REAL-016 | ZB | 复用设备 | real-blocked-recovery | 在 `demo-test` 实例上直接把 `DVC19218A412EA1` 的真实 `credential_ref_in_band/credential_refs/access_points[].credential_ref` 注入为不存在的 `missing-real-zb-cli-081400` 后，从真实 `ZB` 入口发起 `request_id=zb-real-20260601-0036`，同步仍受理成功，但异步 callback、`task_id=entv2_1780272823448954719`、`device_v2_store_run` 与实体都一致收敛到 `collect/device_collection2 -> D2_STORE_DATASET_FAILED`，同时实体落成 `store/blocked/pending` 且 `sync_to_v1_status/monitor_push_status` 被清空、agent 侧 `ping/snmp` 任务更新时间保持在 `2026-06-01 07:46:38` 不变；随后恢复原凭据并以 `request_id=zb-real-20260601-0037` 重放，真实回到 `done/success/success`，并刷新 agent 配置内容为 `ping=192.168.100.19`、`snmp=udp://192.168.100.19:161`、`device_id=DEV20260531000004`。 | passed-real |
| REAL-017 | ZB / sync_to_v1 | 复用设备 | real-content-finding | 对 `DVC19218A412EA1 -> DEV20260531000004` 的真实成功链路做内容级深校验时，发现 `device_v2` 已明确携带 `login_method=ssh`、`in_band_login_method=ssh`，但旧 `device` 表中的 `login_method` 仍为空，说明 `sync_to_v1` 在“已存在 V1、刷新主字段”路径下漏同步了 `login_method`；同轮也确认 `device_to_secret` 为空是当前设计行为，因为 `ingest_cli_B02A796CC80D` 仅通过 `credential_vault_catalog` 被接受为 `SecretHub引用`。 | finding-real |
| REAL-018 | ZB / sync_to_v1 | 复用设备 | real-content-fix | 给 `buildDeviceV1SyncReqFromDeviceV2` 补齐 `login_method/in_band_login_method/access_points` 到 `device_v1` 的映射并补上失败回归测试后，重启 `demo-test` 后端并回放真实请求 `request_id=zb-real-20260601-0038`，确认 callback 继续成功、`DEV20260531000004.login_method` 从空值真实更新为 `ssh`，同时 `hostname=localhost.localdomain`、`in_band_ip=192.168.100.19`、`system_version=8` 保持正确，agent 侧 `ping/snmp` 任务也继续带有正确的 `device_id=DEV20260531000004` 与目标地址。 | passed-real |
| REAL-019 | 页面store / sync_to_v1 | 复用设备 | real-content-verify | 在同一修复上线后，对页面真实 `store/start` 回放 `task_id=entv2_1780273816107082811`，确认 `DVCBAEFDC43A8F3 -> DEV20260531000005` 这条共享 `sync_to_v1` 路径也会把旧 `device.login_method` 从空值真实补成 `ssh`，同时 `hostname=debian`、`in_band_ip=192.168.100.15`、`system_version=12` 继续保持正确，agent 侧 `ping/snmp` 任务刷新到 `2026-06-01 08:30:22` 且仍带有正确的 `device_id=DEV20260531000005` 与目标地址。 | passed-real |
| REAL-020 | ZB / 页面store | 复用设备 | real-sync-content-verify | 对两条真实成功链路 `DVC19218A412EA1 -> DEV20260531000004` 与 `DVCBAEFDC43A8F3 -> DEV20260531000005` 继续做 `sync_to_v1` 明细级核对，确认 `device_v2` 的 `sync_to_v1_message` 与 `device_interface / device_interface_local_macs / ip_address / mac / device_collection2_fact_latest` 实际内容一致：前者真实落成 `updated=4 / ip=1 / mac=4`，后者真实落成 `updated=2 / ip=1 / mac=2`，且 warning 中被忽略的 loopback、link-local 与未匹配 prefix 地址也都能在采集事实中找到对应证据。 | passed-real |
| REAL-021 | 页面store | 混合批次 | real-batch-partial-recovery | 在 `demo-test` 实例上对双设备真实批次 `["DVC19218A412EA1","DVCBAEFDC43A8F3"]` 执行页面 `store/start`：先把 `DVCBAEFDC43A8F3` 的 `credential_ref_in_band/credential_refs/access_points[].credential_ref` 注入为不存在的 `missing-batch-cli-0835` 后回放 `task_id=entv2_1780274743420960269`，确认批次总态真实收敛为 `partial`，其中 `DVC19218A412EA1` 成功、`DVCBAEFDC43A8F3` 阻断于 `device_collection2 -> D2_STORE_DATASET_FAILED`，且 agent 侧只刷新成功设备 `192.168.100.19` 的 `ping/snmp` 任务，不刷新阻断设备 `192.168.100.15`；随后恢复原凭据并再次回放 `task_id=entv2_1780274779435409696`，确认整批真实回到 `success`，两台设备实体都收敛为 `done/success/success`，且四条 agent 任务全部刷新。 | passed-real |
| REAL-022 | ZB | 混合批次 | real-batch-partial-recovery | 在 `demo-test` 实例上对同一双设备真实 ZB 批次 `request_id=zb-real-20260601-0039`/`zb-real-20260601-0040` 执行对照：先把 `DVCBAEFDC43A8F3` 的 `credential_ref_in_band/credential_refs/access_points[].credential_ref` 注入为不存在的 `missing-zb-batch-cli-0902`，再从真实 ZB 入口提交 2 台设备，确认 callback 顶层真实收敛为 `ZB_STORE_PARTIAL_FAILED`，共用 `task_id=entv2_1780275120276333407`，其中 `DVC19218A412EA1` 成功、`DVCBAEFDC43A8F3` 阻断于 `device_collection2 -> D2_STORE_DATASET_FAILED`，实体分别收敛为 `done/success/success` 与 `store/blocked/pending`，且 agent 侧只刷新成功设备 `192.168.100.19` 的 `ping/snmp` 任务到 `08:52:08`，不刷新阻断设备 `192.168.100.15`；随后恢复原凭据并以 `request_id=zb-real-20260601-0040` 重放，确认 callback、`task_id=entv2_1780275175629524346`、实体与 agent 四边全部真实回到 `success`，两台设备对应的 `ping`/`snmp-passthrough` 任务都刷新到 `08:53:09-08:53:10`，且任务内容继续保持 `192.168.100.19 -> DEV20260531000004`、`192.168.100.15 -> DEV20260531000005`，与页面混合批次 `REAL-021` 的批次总态和逐设备行为一致。 | passed-real |
| REAL-023 | 页面store / 页面视图 | 混合批次 | real-view-finding | 对 `REAL-021` 与 `REAL-022` 的真实 mixed batch 再做页面只读接口校验时，发现 `GET /api/v1/device/v2/tasks/entv2_1780275120276333407/summary` 存在批次级状态自相矛盾：`task.overall_status=partial`、`run_summary.blocked=1 failed=0 success=1`，但 `process_result.overall_status=failed` 且 `summary.failed=2 blocked=0`，说明页面 summary 错把单设备 primary run 的 `process_result` 当成了整个批次的结果。 | finding-real |
| REAL-024 | 页面store / 页面视图 | 混合批次 | real-view-fix | 给 `buildMinimalStoreTaskSummary` 增加 mixed batch 聚合逻辑并补上回归测试后，重启 `demo-test` 后端再回查真实任务 `entv2_1780275120276333407` 与 `entv2_1780275175629524346`，确认 partial 批次页面 summary 已收敛为 `process_result.overall_status=blocked`、`summary.success=1 blocked=1 failed=0`、headline=`部分设备采集验证未完成，请查看单设备详情。`，恢复成功批次则收敛为 `process_result.overall_status=success`、`summary.success=2`，两者都与 DB 中的 `run_summary/entity` 及 agent 刷新结果一致。 | passed-real |
| REAL-025 | 页面store / 页面视图 | 混合批次 | real-view-verify | 对 `REAL-022` 的 mixed batch 继续做页面只读接口内容级核对时，确认 `GET /api/v1/device/v2/tasks/{task_id}/runs`、`/observations` 与 `/collection-plans` 的真实返回和 MySQL 中 `device_v2_store_run`、`device_v2_observation` 一致：partial 批次 `entv2_1780275120276333407` 仅成功设备 `DVC19218A412EA1` 产生 12 条 observation、阻断设备无 observation，且下一步计划准确收敛为 `retry_store/noop`；恢复批次 `entv2_1780275175629524346` 则真实返回 21 条 observation，并与两台设备的采集事实数量对齐。 | passed-real |
| REAL-026 | 页面store / 页面视图 | 复用设备 | real-projection-fix | 在继续校验页面 `detail/list` 时，发现真实接口把 `DVC19218A412EA1` 与 `DVCBAEFDC43A8F3` 的 `created_at/updated_at` 错投影成 `0001-01-01T00:00:00Z`，而 MySQL 中 `entity_instance` 与 `platform_devices_v2` 均有正确时间；给 `entityToDeviceV2Projection` 补齐时间戳映射并加回归测试后，重启 `demo-test` 后端回查 `GET /api/v1/device/v2/{code}` 与 `/api/v1/device/v2/list`，确认两台设备都真实返回 `created_at=2026-05-31 11:23:34.xxx`、`updated_at=2026-06-01 08:53:10.xxx`，与 DB 内容一致。 | passed-real |
| REAL-027 | 页面store / 监控 runtime | 复用设备 | real-runtime-finding | 对 `REAL-022` 的两台真实成功设备继续做 agent 运行态深校验时，发现 `GET /api/v2/platform/monitoring/tasks/{projection_id}/runtime` 已明确返回 `collect_agent-001_ping-basic_192_168_100_19_161` 与 `...100_15_161` 为 `running`，但两条 `collect_agent-001_snmp-passthrough_...` 已真实失败，错误分别是 `no route to host` 与 `connection refused`；与此同时，`device_v2` 页面详情仍保持 `process_result.overall_status=success`、`monitor_push_status=success`，而在执行真实 `tasks:sync-agent-snapshot` 之后，`view_mode=runtime` 也只显示 `sync_status=in_sync / business_status=normal` 的配置对账结果，不透出 `status/message/inputs` 运行失败信息，说明当前设备页与 runtime 列表都没有把 agent 真实运行健康度带回前台。 | finding-real |
| REAL-028 | 页面store / 监控 runtime | 复用设备 | real-runtime-fix | 给 `view_mode=runtime` 的读模型补上 agent 实时运行态合并逻辑，并让 `agentTelegrafTaskQuerierImpl` 直接复用 bidi `CallController/ResolveControllerIDForAgent` 后，列表不再只显示 `sync_status/business_status`，而会把 `runtime_status/runtime_message/runtime_last_updated/runtime_inputs` 写入 `ownership`，并同步落到 `task_nodes[0].status/message/last_updated/inputs`；针对性回归测试已覆盖 `running` 与 `failed(connection refused)` 两类真实症状。 | passed-real |
| REAL-029 | 页面store / 监控 runtime | 复用设备 | real-runtime-verify | 重启 `demo-test` 后端后重新回查 `GET /api/v2/platform/monitoring/tasks?view_mode=runtime&agent_code=agent-001&page=1&page_size=50`，确认四条真实任务已经在列表里直接透出运行健康度：`collect_agent-001_ping-basic_192_168_100_15_161` 与 `...100_19_161` 真实显示 `runtime_status=running`、`last_updated=2026-06-01T09:24:20+08:00`，两条 `collect_agent-001_snmp-passthrough_...` 真实显示 `runtime_status=failed`，并带回完整错误消息 `connection refused` / `no route to host`、对应 `inputs[].last_error` 与 `last_gather_at`；同时 `sync_status=in_sync`、`business_status=normal` 仍保留，说明配置对账状态和 agent 运行健康度已经在同一列表视图里并排可见。 | passed-real |
| REAL-030 | 页面store / 设备读模型 | 复用设备 | real-runtime-health-fix | 在 `device_v2` 详情与列表里追加 `runtime_health` 读时聚合后，最初真实回查发现聚合逻辑虽已实现，但主程序 `DeviceV2API` 的启动装配没有把 `MonitoringTaskProjectionServiceV2` 注进去，导致 `GET /api/v1/device/v2/{code}` 与 `/list` 仍然完全不返回 `runtime_health`；补齐 `cmd/wire_gen.go` 的注入后，重启 `demo-test` 后端并重新回查，确认 `DVC19218A412EA1` 与 `DVCBAEFDC43A8F3` 都会按 `device_v1_code -> task_subject -> runtime projection` 的真实链路返回 `runtime_health`。 | passed-real |
| REAL-031 | 页面store / 前端视图 | 复用设备 | real-runtime-health-verify | 在 `device_v2` 前端类型与三处页面展示补齐 `runtime_health` 后，继续用真实设备 `DVC19218A412EA1` 与 `DVCBAEFDC43A8F3` 回查接口，确认虽然两台设备的 `monitor_push_status` 仍为 `success`，但 `runtime_health` 已真实收敛为 `degraded`，并分别带出 `no route to host` 与 `connection refused`；对应前端现在会把“监控已下发”和“Agent 运行态”分开显示，`MonitoringCenterDev` 也不再因为 `monitor_push_status=success` 就把运行失败设备误当成健康设备过滤掉。 | passed-real |
| REAL-032 | 页面store / 前端视图 | 复用设备 | real-active-route-finding | 在进一步做浏览器级真验收时，发现当前真实路由 `/#/device/device-v2-adapt-workbench` 实际挂载的是 `DeviceV2AdaptWorkbenchRebuilt.vue`，而之前补 `runtime_health` 的是旧版 `DeviceV2AdaptWorkbench.vue`，导致 API 已经返回 `runtime_health`，但活跃工作台页面仍看不到“监控已下发 / Agent 运行态 / 真实失败消息”。浏览器 smoke 也因此先稳定红在活跃工作台断言。 | finding-real |
| REAL-033 | 页面store / 前端视图 | 复用设备 | real-active-route-fix | 给真实活跃组件 `DeviceV2AdaptWorkbenchRebuilt.vue` 补上 `monitor_push_status + runtime_health` 展示，并新增浏览器 smoke `OneOPS-UI/scripts/d2-runtime-health-smoke.cjs`：脚本会先从真实 API 读取 `DVC19218A412EA1` 当前 `runtime_health.message`，再通过真实登录、真实路由 `/#/device/device-v2-adapt-workbench?codes=DVC19218A412EA1&code=DVC19218A412EA1` 校验页面逐字展示同一条运行态消息。最新真验收已通过，并生成证据图 `docs/openclaw-autodev/evidence/d2-fe/D2-RUNTIME-HEALTH-SMOKE/20260601-021804-adapt-workbench-runtime-health.png`。 | passed-real |
| REAL-034 | 页面store / 前端视图 | 复用设备 | real-monitoring-center-verify | 在同一支浏览器 smoke 中继续验证真实 `MonitoringCenterDev` 路由 `/#/monitor-panel/monitoring-center`：脚本先用真实 API 取 `DVC19218A412EA1` 当前 `runtime_health.label/message`，再真实登录、进入监控中心、按设备编码筛选，确认左侧设备导航会逐字展示相同的运行态标签和完整失败消息，而不是只保留 `monitor_push_status=success`。最新真验收已通过，并生成证据图 `docs/openclaw-autodev/evidence/d2-fe/D2-RUNTIME-HEALTH-SMOKE/20260601-022656-monitoring-center-runtime-health.png`。 | passed-real |
| REAL-035 | 页面store / 前端视图 | 复用设备 | real-management-detail-verify | 在同一支浏览器 smoke 中继续验证真实 `/#/device/device-v2-management?keyword=DVC19218A412EA1&code=DVC19218A412EA1`：页面会按真实路由直接打开 `DVC19218A412EA1` 的详情抽屉，并逐字展示与 API 完全一致的 `monitorStatus=监控已下发`、`runtimeStatus=运行异常` 和完整 `runtime_health.message`，同时保留“基础信息 / 网络与位置 / 技术信息”结构。最新真验收已通过，并生成证据图 `docs/openclaw-autodev/evidence/d2-fe/D2-RUNTIME-HEALTH-SMOKE/20260601-024320-management-runtime-health.png`。 | passed-real |
| REAL-036 | 页面store / 前端视图 | 复用设备 | real-blocked-projection-fix | 在 `demo-test` 实例上继续复用 `REAL-015` 的脏凭据设备 `DVCBAEFDC43A8F3`，先确认旧实现下 `task_id=entv2_1780282090045742898` 已真实收敛为 `partial/failed`，但 `detail/list` 仍残留上一轮成功的 `process_result/sync_to_v1_status/monitor_push_status`；随后修复 `entity` 写回只清 `attributes`、未清 `metadata` 的问题后，重新以真实 `store/start(async=false)` 回放 `task_id=entv2_1780282602876510363`，确认同一台设备在 blocked 后真实收敛为 `stage_status=blocked、manage_status=pending、process_result=null、sync_to_v1_status=null、monitor_push_status=null`，同时 agent 侧 `collect_agent-001_ping-basic_192_168_100_15_161` 与 `snmp-passthrough` 任务更新时间保持在旧值不变。 | passed-real |
| REAL-037 | 页面恢复写入 / 页面视图 | 复用设备 | real-restore-write-finding | 在修复 `REAL-036` 后继续用真实备份 payload `/tmp/DVCBAEFDC43A8F3.restore.update.json` 走 `PUT /api/v1/device/v2/DVCBAEFDC43A8F3?base_ref_mode=warn` 做恢复时，发现旧实现会把备份里的 `process_result/sync_to_v1_status/monitor_push_status/workflow_trace` 重新写回 `entity attributes/metadata`，导致刚清干净的 blocked 设备又被手工恢复数据重新污染成“页面看似成功、实际 agent runtime 仍失败”的假成功态。 | finding-real |
| REAL-038 | 页面恢复写入 / 页面视图 | 复用设备 | real-restore-write-fix | 给 `buildEntitySourceForWrite` 增加后台派生字段净化后，重启 `demo-test` 后端并再次对同一份真实恢复 payload 发起 `PUT /api/v1/device/v2/DVCBAEFDC43A8F3?base_ref_mode=warn`，确认即使 payload 自带旧成功态字段，`detail/list` 也都会真实返回 `process_result=null、sync_to_v1_status=null、monitor_push_status=null`，同时 `runtime_health` 仍按当前 agent 运行态维持 `degraded`，三页浏览器 smoke 也再次通过，证据图分别为 `20260601-030234-adapt-workbench-runtime-health.png`、`20260601-030234-monitoring-center-runtime-health.png`、`20260601-030234-management-runtime-health.png`。 | passed-real |
| REAL-039 | 页面store / 监控 runtime / 设备读模型 | 复用设备 | real-network-restored-verify | 在恢复 `controller/agent -> 目标设备` 网络后，对 `DVCBAEFDC43A8F3` 重新发起真实 `store/start(async=false)` 并得到 `task_id=entv2_1780284269709891360` 成功收敛后，继续对 `detail/list/runtime list/单条 runtime` 四边做内容级核对：`device_v2` 详情与列表都真实显示 `manage_status=success`、`runtime_health=degraded`，并明确带回 `agent udp://192.168.100.15:161 ... connection refused`；同时 `view_mode=runtime` 与两条单独 `/runtime` 也真实显示 `ping-basic_192_168_100_15_161=running`、`snmp-passthrough_192_168_100_15_161=failed`，说明网络恢复后平台没有再误报 `healthy`，而是把 agent 的真实 SNMP 失败完整透出。 | passed-real |
| REAL-040 | 页面store / 前端视图 | 复用设备 | real-network-restored-frontend-verify | 在 `REAL-039` 的同一台真实设备 `DVCBAEFDC43A8F3` 上，继续执行浏览器 smoke `OneOPS-UI/scripts/d2-runtime-health-smoke.cjs` 并把 `D2_RUNTIME_HEALTH_DEVICE_CODE` 切到该设备，确认工作台、监控中心、设备管理详情三页都会逐字展示与真实 API 一致的 `runtime_health.label/message=运行异常 / connection refused`，同时生成最新证据图 `20260601-033053-adapt-workbench-runtime-health.png`、`20260601-033053-monitoring-center-runtime-health.png`、`20260601-033053-management-runtime-health.png`。 | passed-real |
| REAL-041 | 页面store / 网络设备 | 路由器 | real-network-snmp-success-baseline | 在 `demo-test` 实例上选取真实网络设备 `DVCE1EE3F7D394C` 作为“SNMP 应该成功”的基线样本：该设备画像明确为 `H3C / Comware / VSR1000`，`in_band_ip=172.32.2.15`，`credential_ref_in_band=ingest_cli_B49951FB1B3B`，`snmp_credential_ref=ingest_snmp_11B7AEDE3505`，既有真实链路 `task_id=entv2_1780207816239984460` 使用 `contract_key=h3c_comware` 成功采到 100 条事实，且当前 agent runtime 真实显示 `collect_agent-001_ping-basic_172_32_2_15_161=running`、`collect_agent-001_snmp-passthrough_172_32_2_15_161=running`。 | passed-real |
| REAL-042 | 页面store / 网络设备 / 设备读模型 | 路由器 | real-network-rerun-finding | 在 `REAL-041` 的同一台健康网络设备 `DVCE1EE3F7D394C` 上重新发起真实 `store/start(async=false)` 并得到 `task_id=entv2_1780284990466059154` 总体 `success` 后，暴露出两处新的真实不一致：其一，请求选项明确带了 `device_collection2.block_on_partial=true`，但 `device_run.device_collection2.status` 仍为 `partial`、`reason=run_partial`，最终仍被 `decision=D2_STORE_SUCCESS` 放行；其二，回放完成后 `device_v2` 详情被真实清空成 `sync_to_v1_status=null、monitor_push_status=null、process_result=null`，同时 `runtime_health` 与两条单条 `/runtime` 又退回 `unknown / 未发现该任务对应的 input 运行状态`，说明“健康网络设备重复回放”目前仍存在 partial 守门语义偏差与成功后投影/运行态丢失问题。 | finding-real |
| REAL-043 | 页面store / 网络设备 / 设备读模型 | 路由器 | real-manage-projection-fix | 针对 `REAL-042` 中“task 已走到 manage success，但 `entity_instance/platform_devices_v2/detail` 仍丢失 `sync_to_v1_status/monitor_push_status/process_result`”的问题，定位到 `updateDeviceV2ManageProjection -> DeviceV2Srv.Update -> clearDeviceV2ManagedProjectionWriteFields` 会把 manage 派生字段在通用写路径里再次清空；补上 `DeviceV2Srv.UpdateManageProjection` 专用写口并让 `persistManageResults` 改走该入口后，重启 `:8280` 并回放真实网络设备 `task_id=entv2_1780286419628248093`，确认 `entity_instance`、`platform_devices_v2`、`detail/list` 都真实收敛为 `sync_to_v1_status=skipped`、`monitor_push_status=success`、`process_result=success`。 | passed-real |
| REAL-044 | 页面store / 网络设备 / task-runs | 路由器 | real-store-run-manage-fix | 在 `REAL-043` 修复后继续检查 `task_id=entv2_1780286419628248093` 的 `/api/v1/device/v2/tasks/{id}/runs` 与 `device_v2_store_run.summary_json`，发现 source/projection 已正确但 per-device run 仍停留在 `persist_core` 视角，缺失 `sync_to_v1_status/monitor_push_status/process_result`；补齐 `persistManageResults` 对 `device_v2_store_run` 的 manage 回写后，再次重启并回放真实网络设备得到 `task_id=entv2_1780286910649828497`，确认 `device_v2_store_run.summary_json`、`/tasks/{id}/runs`、`detail/list` 四边都真实带回 `sync_to_v1_status=skipped`、`monitor_push_status=success`、`process_result=success`。 | passed-real |
| REAL-045 | 页面store / 网络设备 / partial 守门 | 路由器 | real-partial-gate-semantics-fix | 针对 `REAL-042` 暴露出的 `block_on_partial` 语义偏差，先通过单测与代码核对确认旧实现虽然在 `dc2_primary/dc2_only` 下默认没有注入 `block_on_partial`，但真正的 store gate 又只会在 `required_failed_dataset_keys` 非空时阻断 partial；随后修正生产默认注入，并将文档、配置注释与实现统一成“在 `dc2_primary/dc2_only` 下默认开启 `block_on_partial`，但只对缺少 required dataset 的 partial 进行阻断”。 | passed-real |
| REAL-046 | 页面store / 网络设备 / partial 守门 | 路由器 | real-partial-gate-verify | 重启 `:8280` 后对真实网络设备 `DVCE1EE3F7D394C` 再次发起 `store/start(async=false)`，得到 `task_id=entv2_1780287684079230618`；结果显示 task options 已真实携带 `device_collection2.block_on_partial=true`，同时本次 `device_collection2.status=partial`、`reason=run_partial` 仍被 `decision=D2_STORE_SUCCESS` 正常放行，原因是该 partial 并未产生 `required_failed_dataset_keys`，从而验证“默认严格 + 按 required dataset 判阻断”这套新语义已经在真实链路中生效。 | passed-real |
| REAL-047 | 页面store / DC2 profile / required datasets | 多设备类型 | real-profile-required-dataset-fix | 为避免 `block_on_partial` 在设备类型增多后退化成“靠全局默认猜关键 dataset”，给 `device_collection2.collection_profiles` 补齐 `required_dataset_keys` 语义，并让 `collectionplan.Resolve -> runMinimalDeviceV2StoreDC2Sidecar -> minimalDeviceV2DC2RequiredDatasetKeys` 整链优先消费 profile 级 required 清单；同时补上配置校验，要求 `required_dataset_keys` 必须是 `dataset_keys` 的子集。 | passed-code |
| REAL-048 | 页面store / Nacos / DC2 profile | 路由器 | real-profile-required-dataset-network-verify | 将 `cipher-aes-start-config@ONEOPS_PROD` 真实更新为带 `required_dataset_keys` 的 `collection_profiles` 并重启 `:8280` 后，再次回放网络设备 `DVCE1EE3F7D394C`，确认真实 API 返回的 `device_runs[0].device_collection2` 中已经带回 `collection_plan_profile_id=net-h3c-comware` 与 `required_dataset_keys=[\"snmp_sys_descr\"]`，同时 `status=partial` 仍按前述规则正常放行。 | passed-real |
| REAL-049 | 页面store / Nacos / DC2 profile | 服务器 | real-profile-required-dataset-server-verify | 在同一份真实 Nacos 配置生效后，回放服务器样本 `DVCBAEFDC43A8F3`，确认真实 API 返回的 `device_runs[0].device_collection2` 中已经带回 `collection_plan_profile_id=srv-linux-generic` 与 `required_dataset_keys=[\"linux_hostname\",\"linux_os_release\"]`，并且整条 `server_linux` 采集链路继续 success，没有因为 profile 级 required 清单引入回归。 | passed-real |
| REAL-050 | 页面store / Nacos / required partial | 路由器 | real-required-partial-finding | 为验证 profile 级 `required_dataset_keys` 的真实阻断效果，临时将 `ONEOPS_PROD/cipher-aes-start-config` 中 `net-h3c-comware.required_dataset_keys` 收紧为 `[\"snmp_sys_descr\",\"cli_interface_brief\"]` 并重启 `:8280`，随后回放真实网络设备 `DVCE1EE3F7D394C` 得到 `task_id=entv2_1780291429930770703`：原始 `device_collection2_run_device.dataset_results_json` 已明确显示 `cli_interface_brief.status=partial`，但最终 `device_v2` 任务仍真实收敛为 `run_status=success`、`decision_code=D2_STORE_SUCCESS`，说明“required dataset 处于 partial”在 store gate 前被错误放掉了。 | finding-real |
| REAL-051 | 页面store / Nacos / required partial | 路由器 | real-required-partial-fix | 先在 `minimalDeviceV2DC2SummarizeRunDeviceDatasets(...)` 中把 `required dataset + partial` 也纳入 `required_failed_dataset_keys`，再修复 `run.SetDeviceCollectionProjection(...)` / `DeviceV2StoreRunCollectionReport` 未把 `failed_dataset_keys / partial_dataset_keys / issue_dataset_keys / required_failed_dataset_keys` 带入最终 summary 的问题后，重启 `:8280` 并在同一份加严 profile 下再次回放 `DVCE1EE3F7D394C` 得到 `task_id=entv2_1780291654524693201`：这次真实返回已稳定收敛为 `run_status=blocked`、`decision_code=D2_STORE_DC2_REQUIRED_FAILED`、`required_failed_dataset_keys=[\"cli_interface_brief\"]`，DB 中实体也真实落成 `store/blocked/pending`，且 `sync_to_v1_status / monitor_push_status` 均为 `NULL`。 | passed-real |
| REAL-052 | 页面store / Nacos / baseline-restore | 路由器 | real-required-partial-baseline-restore | 完成 `REAL-051` 后，将 `ONEOPS_PROD/cipher-aes-start-config` 中 `net-h3c-comware.required_dataset_keys` 恢复为基线 `[\"snmp_sys_descr\"]` 并重启 `:8280`，再对同一台真实网络设备 `DVCE1EE3F7D394C` 执行 `store/start(async=false)` 得到 `task_id=entv2_1780291875965452514`：真实返回显示 `collection_plan_profile_id=net-h3c-comware`、`required_dataset_keys=[\"snmp_sys_descr\"]`、`partial_dataset_keys=[\"cli_interface_brief\"]`、`required_failed_dataset_keys=[]`，最终重新收敛为 `decision_code=D2_STORE_SUCCESS`、`process_result=success`；同时 DB 中实体真实为 `done/success/success`、`sync_to_v1_status=skipped`、`monitor_push_status=success`，说明“临时加严验证 -> 修复 -> 恢复基线”这条链已经闭环。 | passed-real |
| REAL-053 | ZB / 网络设备 | 路由器 | real-zb-network-baseline | 以真实网络设备 `DVCE1EE3F7D394C` 为样本，复用现网已经验证过可用的 `admin / 321@jierui / admin@123` 接入口径，通过真实 `/api/v1/external/adapt/zb` 发起 `request_id=zb-real-20260601-0041`：同步响应先返回 `SuccessSaveBuildDeviceInStep`，随后 `external_request_log.callback_detail` 真实回落为 `ZB_STORE_SUCCESS`，内部 task `entv2_1780292399620021336` 与 callback `summary_v2` 一致显示 `device_v1_code=DEV20260531000009`、`sync_to_v1_status=skipped`、`monitor_push_status=success`，说明在基线 profile 下 ZB 入口也能真实走完整条 `detect/collect/store/manage/callback` 成功链。 | passed-real |
| REAL-054 | ZB / Nacos / required partial | 路由器 | real-zb-required-partial-block | 在 `REAL-053` 的同一台设备上，临时将 `ONEOPS_PROD/cipher-aes-start-config` 中 `net-h3c-comware.required_dataset_keys` 收紧为 `[\"snmp_sys_descr\",\"cli_interface_brief\"]` 并重启 `:8280`，再通过真实 ZB 入口发起 `request_id=zb-real-20260601-0042`：同步仍返回基础信息受理成功，但异步 `external_request_log.callback_detail` 最终真实收敛为 `ZB_STORE_ALL_FAILED`，且 `summary_v2[0].error_code=D2_STORE_DC2_REQUIRED_FAILED`、`origin_stage=collect`、`task_id=entv2_1780292492927815686`；同时 DB 中 `DVCE1EE3F7D394C` 真实落成 `store/blocked/pending` 且 `sync_to_v1_status / monitor_push_status = NULL`，证明“required partial 阻断”已经贯通到 ZB callback 闭环，不只是页面入口有效。 | passed-real |
| REAL-055 | ZB / Nacos / baseline-restore | 路由器 | real-zb-required-partial-baseline-restore | 完成 `REAL-054` 后，将 `net-h3c-comware.required_dataset_keys` 恢复为基线 `[\"snmp_sys_descr\"]` 并重启 `:8280`，再次通过真实 ZB 入口回放 `request_id=zb-real-20260601-0043`：一开始 `external_request_log` 仍显示同步受理成功，随后 callback 真实回落为 `ZB_STORE_SUCCESS`，`summary_v2` 恢复为 `manage_status=success / sync_to_v1_status=skipped / monitor_push_status=success`，内部 task 为 `entv2_1780292548330315485`，DB 也重新收敛为 `done/success/success`；这说明 ZB 侧同样完成了“临时加严验证 -> 严格阻断 -> 恢复基线”的完整闭环。 | passed-real |
| REAL-056 | ZB / 混合批次 / required partial | 服务器 + 路由器 | real-zb-mixed-batch-partial | 在重新开启 `net-h3c-comware.required_dataset_keys=[\"snmp_sys_descr\",\"cli_interface_brief\"]` 的严格 profile 后，通过真实 ZB 入口提交包含服务器 `DVC19218A412EA1` 与路由器 `DVCE1EE3F7D394C` 的混合批次 `request_id=zb-real-20260601-0044`：同步阶段两台设备都返回 `SuccessSaveBuildDeviceInStep`，但异步 callback 真实收敛为 `ZB_STORE_PARTIAL_FAILED`，共享内部 `task_id=entv2_1780292809168618395`；其中服务器行保持 `status=success / manage_status=success / sync_to_v1_status=skipped / monitor_push_status=success`，路由器行则真实返回 `error_code=D2_STORE_DC2_REQUIRED_FAILED`、`origin_stage=collect`、`retryable=true`，DB 也对应收敛为一台 `done/success/success`、一台 `store/blocked/pending`，证明 `required partial` 在 ZB 混合批次下也能稳定形成“批次 partial + 逐设备分化”的真实闭环。 | passed-real |
| REAL-057 | ZB / 混合批次 / baseline-restore | 服务器 + 路由器 | real-zb-mixed-batch-baseline-restore | 将 `ONEOPS_PROD/cipher-aes-start-config` 恢复到基线 `net-h3c-comware.required_dataset_keys=[\"snmp_sys_descr\"]` 并重启 `:8280` 后，再次通过真实 ZB 入口回放同一混合批次 `request_id=zb-real-20260601-0045`：callback 真实回落为 `ZB_STORE_SUCCESS`，共享内部 `task_id=entv2_1780292998481338422`，`summary_v2` 中两台设备都收敛为 `status=success / manage_status=success / sync_to_v1_status=skipped / monitor_push_status=success`；同时 DB 中 `DVC19218A412EA1` 与 `DVCE1EE3F7D394C` 都真实落成 `done/success/success`，说明 ZB 混合批次同样完成了“严格阻断 -> 恢复基线 -> 全成功”闭环。 | passed-real |
| REAL-058 | ZB / 监控 runtime / mixed batch | 服务器 + 路由器 | real-zb-mixed-batch-runtime-unknown | 在 `REAL-057` 的同一批真实成功链路完成后，继续对四条监控任务 `collect_agent-001_ping-basic_192_168_100_19_161`、`collect_agent-001_snmp-passthrough_192_168_100_19_161`、`collect_agent-001_ping-basic_172_32_2_15_161`、`collect_agent-001_snmp-passthrough_172_32_2_15_161` 调用单条 `/api/v2/platform/monitoring/tasks/{projection_id}/runtime`，发现虽然任务都 `found=true`、`enabled=true` 且版本已经刷新到本轮 `1780293019`，但四条 runtime 全部真实返回 `status=unknown`、`message=任务已存在，但未发现该任务对应的 input 运行状态`；这说明“monitor_push 成功 + projection version 已刷新”并不天然等价于 agent runtime 已经形成可读健康态，后续仍需继续追这条真实运行时缺口。 | finding-real |
| REAL-059 | ZB / 监控 runtime / mixed batch | 服务器 + 路由器 | real-zb-mixed-batch-runtime-settled | 在 `REAL-058` 之后继续等待 controller/agent runtime 稳定，并再次查询同四条 `/runtime`：这次真实结果已经能返回带哈希后缀的 input 名称，如 `collect_agent-001_ping-basic_192_168_100_19_161_<hash>` 与 `collect_agent-001_snmp-passthrough_172_32_2_15_161_<hash>`，说明现有的 `task_id + "_"` 前缀匹配规则本身可以命中真实 input；同时这些任务会继续随时间演化成 `running` / `failed(no route to host)` / `stale` 等不同状态，证明 `REAL-058` 更可能是“任务刚刷新完成后，input 运行态暂时还未形成”的时序窗口，而不是名称匹配实现错误。 | passed-real |
| REAL-060 | device_v2 读模型 / runtime_health | 路由器 | real-runtime-health-stale-finding | 在 `REAL-059` 的 runtime 已经稳定可读后，继续对 `DVCE1EE3F7D394C` 做 `detail/list` 对照：单条 `/runtime` 已真实显示 `collect_agent-001_snmp-passthrough_172_32_2_15_161 = stale`，但 `GET /api/v1/device/v2/DVCE1EE3F7D394C` 与 `/api/v1/device/v2/list` 仍把该设备聚合成 `runtime_health=healthy`、`message=2 个监控任务正在 agent 正常运行`，说明 `runtime_health` 聚合口径错误地把 `stale` 当成了健康态。 | finding-real |
| REAL-061 | device_v2 读模型 / runtime_health | 路由器 | real-runtime-health-stale-fix | 针对 `REAL-060`，先补失败测试 `TestDeviceV2APIGetByCode_RuntimeHealthDoesNotTreatStaleAsHealthy`，再最小修复 `buildDeviceV2RuntimeHealthSummary(...)`：将 `stale` 从默认“found 即 healthy”分支中拆出，聚合为 `status=degraded / tone=warning`。重启 `:8280` 后真实回查 `GET /api/v1/device/v2/DVCE1EE3F7D394C` 与 `/api/v1/device/v2/list`，现已稳定返回 `runtime_health.status=degraded`、`message=2 个监控任务中有 1 个运行状态超过 5 分钟未更新...`，不再把 stale 误报成 healthy；服务器设备 `DVC19218A412EA1` 仍继续正确显示 `degraded / no route to host`。 | passed-real |
| REAL-062 | 监控 runtime / 时间序列 | 路由器 | real-snmp-runtime-refresh-window | 对 `DVCE1EE3F7D394C` 的 `ping` 与 `snmp` 任务做 3 个时间点、每次间隔 70 秒的真实观测：`t0=14:11:21` 时两条任务都还停在 `last_updated=14:06:24`；到 `t70=14:12:31` 时，`ping` 跳到 `14:11:08` 并保持 `running`，而 `snmp` 仍停在 `14:06:24` 并被判成 `stale`；到 `t140=14:13:41` 时，`ping` 仍停在 `14:11:08`、`snmp` 仍停在 `14:06:24`。这说明 runtime 时间戳并不是按 `interval=60s` 平滑推进，至少对这类网络设备任务存在更慢的刷新窗口。 | finding-real |
| REAL-063 | 监控策略内容 / SNMP passthrough | 路由器 | real-h3c-snmp-oid-mismatch-finding | 继续对 `DVCE1EE3F7D394C` 做内容级核对后发现：其当前 `snmp-passthrough` 任务来自真实策略 `platform_teleabs_strategy.id=10000000-0000-4000-8000-000000000001 (H3C网络设备监控-副本)`，策略里写死了 `cpu_usage=1.3.6.1.4.1.25506.2.6.1.1.1.1.6`、`memory_usage=1.3.6.1.4.1.25506.2.6.1.1.1.1.8`；但在同一环境对目标设备直接执行真实 `snmpget -v2c -c admin@123 172.32.2.15` 时，这两个 OID 都快速返回 `No Such Instance`，而 `RFC1213-MIB::sysUpTime.0` 与 `IF-MIB::ifDescr` 又能正常返回结果。说明当前问题已经不只是运行态时序，而是这台 H3C/Comware/VSR1000 设备实际命中的 SNMP passthrough 策略内容本身存在 OID 适配风险。 | finding-real |
| REAL-064 | 页面store / DC2 画像纠偏 | 路由器 | real-dc2-identity-model-fix | 针对 `REAL-063` 向前追查后确认：`device_collection2` 的真实 facts 同时包含 `physical_entity.model=Fixed SubCard on Board` 与 `device_identity.model=VSR1000`，但旧实现按 `ListFacts(observed_at desc, updated_at desc)` 的返回顺序做“先到先占坑”，导致低质量 physical module 名会抢先写入主画像。补上回归测试并修复 `minimalDeviceV2CollectFromDC2IdentityFact(...)` 后，重启 `:8280` 并对真实设备 `DVCE1EE3F7D394C` 再次执行 `store/start(async=false)` 得到 `task_id=entv2_1780295133192544516`：最终 `entity_instance.attributes.model` 与 `metadata.device_model_name` 都真实收敛为 `VSR1000`，不再停留在 `Fixed SubCard on Board`。 | passed-real |
| REAL-065 | 页面store / 监控策略选择 | 路由器 | real-model-fix-does-not-refine-monitoring-strategy | 在 `REAL-064` 生效后继续对 `DVCE1EE3F7D394C` 做内容级回查：虽然主记录模型已经真实变成 `VSR1000`，但 `platform_monitoring_task_subject` 中 `collect_agent-001_snmp-passthrough_172_32_2_15_161` 仍继续命中 `strategy_id=10000000-0000-4000-8000-000000000001` 这个 H3C 通用副本，而不是更细粒度的型号策略。这说明当前策略集 `4284353d-1233-4022-ad18-871b3d8444c7 (网络监控套件)` 对该设备并不存在 `VSR1000` 专用子策略，单纯修正入库画像并不会自动把监控策略细化到更合适的 OID 口径。 | finding-real |
| REAL-066 | device_v2 读模型 / runtime_health / metric coverage | 路由器 | real-runtime-health-metric-stale-fix | 对 `DVCE1EE3F7D394C` 的 `snmp-passthrough` 真实做内容级核对时，确认任务级 `/runtime` 已是 `running` 且 `last_metrics_count=10`，但 `platform_metric_asset_instance` 中仍有 `cpu_usage / mem_usage / memory_usage / temperature` 4 个声明指标被真实标成 `stale`。针对这种“任务在跑，但声明指标未覆盖”的分叉，先补失败测试 `TestDeviceV2APIGetByCode_RuntimeHealthDegradesWhenMetricAssetsStale`，再把 `device_v2_runtime_health` 改成双层聚合：保留任务级 runtime 优先级，但当同设备绑定任务存在 `metric_asset_instance.status=stale` 时，设备级 `runtime_health` 不再继续显示 `healthy`。 | passed-code |
| REAL-067 | 页面detail / metric coverage / runtime precedence | 路由器 | real-runtime-health-metric-stale-verify | 将新代码重启到 `:8280` 后，对 `agent-001` 真实执行 `POST /api/v2/platform/monitoring/tasks:sync-agent-snapshot`，返回 `observed_metric_asset_count=121`、`stale_metric_asset_count=8`；其中 `collect_agent-001_snmp-passthrough_172_32_2_15_161` 在库里真实收敛为 `observed=20 / stale=4`。随后回查 `GET /api/v1/device/v2/DVCE1EE3F7D394C`，设备级 `runtime_health` 已不再误报成 `healthy`，而是继续按更高优先级的真实任务状态显示 `degraded / 运行状态超过 5 分钟未更新`，说明“任务 stale 优先于指标 stale，只有在任务级健康时才会落到声明指标未覆盖”这套新口径已经生效。 | passed-real |
| REAL-068 | agent runtime / metrics count | 路由器 | real-agent-negative-last-metrics-count-fix | 在继续追 `DVCE1EE3F7D394C` 的真实 runtime 时，发现 `collect_agent-001_snmp-passthrough_172_32_2_15_161` 曾真实返回 `last_metrics_count=-41`。根因定位到 agent `gatherMetrics()` 使用 `len(metricsCh)` 的前后差估算单个 input 的采集量，在 `processMetrics` 并发消费 channel 时会出现负数；先补红灯测试 `TestGatherMetrics_DoesNotReportNegativeMetricCountWhenChannelIsDrainedConcurrently`，再改为由 `inputStatusAwareAccumulator` 自己累计 `AddFields/AddMetric` 次数。定向单测 `go test ./pkg/ops/metrics -count=1` 通过，真实回查同批次 `ping` 任务也已稳定返回 `last_metrics_count=1`，不再出现负数。 | passed-code |
| REAL-069 | agent runtime / 重放后时序 | 路由器 | real-agent-runtime-window-after-reapply | 将带修复的新 agent 重启并重新对 `DVCE1EE3F7D394C` 发起真实 `store/start(async=false)` 后，再次观察到一个容易误判的时序窗口：平台成功下发 `version=1780298108` 后，`ping` 任务很快恢复为 `running / last_metrics_count=1`，但 `snmp` 任务仍会在一段时间内保持 `status=unknown`，且 `platform_metric_asset_instance` 可能暂时停在 `planned=21`。结合 agent 日志与代码进一步确认，这并不等同于“任务没有下发”，而是 `ReplaceAllInputs()` 会先清空 `inputStatus`，且 agent 当前对所有 input 采用串行 `gatherMetrics()`；在 SNMP 任务多、单任务耗时长的现场，这类任务即使在 nominal interval 内也可能还没轮到新一轮采集。 | finding-real |
| REAL-070 | agent runtime / 重放后时序 | 路由器 | real-agent-runtime-old-sample-reappearance | 基于当前通过 Nacos 运行的真实 `:8280` 实例，再次对 `DVCE1EE3F7D394C` 执行页面 `store/start(async=false)`，得到 `task_id=entv2_1780298951581795673`，新版本下发后两条监控任务都更新到 `version=1780298962`。随后做 3 个时间点观测：`t0=2026-06-01 15:29:46 +08:00` 时，`collect_agent-001_ping-basic_172_32_2_15_161` 与 `collect_agent-001_snmp-passthrough_172_32_2_15_161` 都真实返回 `status=unknown`，`GET /api/v1/device/v2/DVCE1EE3F7D394C` 也同步收敛为 `runtime_health=unknown`，且 `platform_metric_asset_instance` 仅有 `ping=planned(9)`、`snmp=planned(21)`；到 `t+70=15:31:20` 时，两条 `/runtime` 都又变成 `running`，但 `last_updated` 统一停在更早的 `2026-06-01T15:27:20+08:00`，不是本轮重放后的时间；到 `t+140=15:32:30` 时，两条任务再一起漂成 `stale`，`last_updated` 仍保持 `15:27:20`，设备级 `runtime_health` 也随之从 `unknown -> healthy -> degraded` 演化，而 metric asset 仍全程停在 `planned`。这说明当前重放后的 `unknown` 窗口不只是“等待新 input 首次采集”，还可能在窗口结束后重新挂回旧 input 的运行态时间戳，因此不能把 `unknown` 之后重新出现的 `running` 直接当成本轮新版本已经完成了新鲜采集。 | finding-real |
| REAL-071 | agent runtime / 重放后时序 | 路由器 | real-agent-runtime-old-sample-race-fix | 针对 `REAL-070`，先在 `ctrlhub/controller/agent/pkg/ops/metrics` 补红灯测试 `TestReplaceAllInputs_DoesNotRestoreStatusFromInFlightGatherBeforeReplace`，再给 `TelegrafInputCollector` 增加 input status 代际保护：`ReplaceAllInputs()` 清空状态时递增 generation，旧一轮 `gatherMetrics()` 在 replace 之后结束时不再允许把过期状态写回当前 `inputStatus`。定向单测 `go test ./pkg/ops/metrics -count=1` 通过后，重启现场 agent 并再次对 `DVCE1EE3F7D394C` 执行真实 `store/start(async=false)`，得到 `task_id=entv2_1780300434210851389`、任务版本 `1780300445`。新的真实观测中，`t0=15:54:21` 与 `t+70=15:55:53` 两条 `/runtime` 都持续保持 `unknown`，不再像 `REAL-070` 那样重新挂回早于本轮重放完成时间的旧样本；到 `t+140=15:57:03` 时，`snmp` 任务开始返回 `running`，且 `last_updated=2026-06-01T15:54:38+08:00` 已晚于本轮 `store/start` 完成时间 `15:54:06`，证明“旧 input 状态回灌当前代”这一层并发问题已被真实压住。与此同时，`ping` 仍保持 `unknown`，`platform_metric_asset_instance` 也仍停在 `planned`，说明剩余 open gap 已收敛为“monitor push 后未自动触发 runtime snapshot / metric asset 同步”，而不再是 agent 旧状态回写。 | passed-real |
| REAL-072 | 页面store / snapshot sync / metric asset | 路由器 | real-store-monitor-push-auto-sync-snapshot-fix | 针对 `REAL-071` 剩余的“monitor push 成功后未自动触发 snapshot / observed metric 同步”，先在 `OneOps/app/device/service/impl` 补红灯测试 `TestTryNotifyMonitorProbeByStrategySelectorSets_SyncsAgentSnapshotAfterSuccessfulApply`，再让 store 入口在自动 apply 成功后按成功 `TaskOperationItems.agent_code` best-effort 触发 `TaskProjectionServiceV2.SyncAgentSnapshot(...)`。定向回归 `go test ./app/device/service/impl -count=1` 与 `go test ./cmd -run TestDoesNotExist -count=1` 通过，并重启当前 Nacos 驱动的 `:8280` 后再次对 `DVCE1EE3F7D394C` 执行真实 `store/start(async=false)`，得到 `task_id=entv2_1780301405928656020`。本轮在 `2026-06-01 16:10:19 +08:00` 完成时，两条单条 `/runtime` 仍都返回 `status=unknown / version=1780301418`，但库里已经自动写出新的 `platform_monitoring_v2_agent_task_snapshot.snapshot_version=1780301419031913491 / synced_at=2026-06-01 16:10:19.032`；同时 `platform_metric_asset_instance` 不再停在 `planned`，而是真实收敛为 `ping=observed(9)`、`snmp=observed(19)+stale(2)`。`GET /api/v1/device/v2/DVCE1EE3F7D394C` 也立即带回 `runtime_health=degraded` 且 message 变成“0 个监控任务正在运行，但 1 个任务下有 2 个声明指标未观测到”，说明 store 路径里的自动 snapshot / metric asset 同步已经生效，剩余滞后只在 agent input runtime 状态本身。 | passed-real |
| REAL-073 | agent runtime / burst replace / 最终代 gather | 路由器 | real-agent-runtime-stale-gather-cycle-stop-fix | 继续沿 `REAL-072` 追现场后发现，问题并不只是“input 开始采集前没有状态”，还包括 burst apply 期间的多次 `ReplaceAllInputs()` 会让一轮旧 generation 的 `gatherMetrics()` 在状态已注定会被丢弃的情况下继续把整轮旧 inputs 跑到底，导致最终代 gather 被排到很后面。先补红灯测试 `TestReplaceAllInputs_StopsStaleGatherCycleSoLatestInputsCanRunPromptly`，再把 `gatherMetrics()` 抽成有序快照执行器，并在每次进入下一条 input 前检查 `inputStatusGeneration`；如果 replace 已发生，就立即终止旧代循环，让挂起的最新一代 gather 尽快启动。定向回归 `go test ./pkg/ops/metrics -count=1` 通过；重启现场 agent 后再次对 `DVCE1EE3F7D394C` 执行真实 `store/start(async=false)`，得到 `task_id=entv2_1780303994867650312`。新的真实观测里，`t0=16:53:26` 时两条 `/runtime` 仍是 `unknown`，但 `ping` 已在 `t+20=16:53:46` 提前收敛为 `running / last_updated=2026-06-01T16:53:35+08:00`，不再像上一轮那样明明已开始采集却继续卡在 `unknown`；`snmp` 则在 `t+100=16:55:07` 收敛为 `running`，设备级 `runtime_health` 也同步回到 `healthy`。这说明“旧代 gather 白跑到底、把最终代挤到后面”这一层已经被真实压缩，剩余窗口主要只和该 input 自身实际轮到的时间有关。 | passed-real |
| REAL-074 | agent runtime / per-input 时间戳口径 | 路由器 | real-agent-runtime-per-input-timestamp-fix | 在 `REAL-073` 的真实回放中继续比对时间戳后，又暴露出另一处容易误导判断的口径问题：虽然 `snmp` 直到 `t+100` 才首次从 `unknown` 变成 `running`，接口里却和更早出现的 `ping` 一样返回 `last_updated=2026-06-01T16:53:35+08:00`。根因定位到 `gatherMetrics()` 在进入整轮循环时只取一次 `now`，导致更晚完成的 input 也沿用了整轮起点时间。先补红灯测试 `TestGatherMetricsSnapshot_RecordsPerInputTimestampInsteadOfWholeCycleStart`，再把成功/失败状态回写改成使用各 input 自己的完成时刻。定向回归 `go test ./pkg/ops/metrics -count=1` 通过；重启现场 agent 后再次对 `DVCE1EE3F7D394C` 执行真实 `store/start(async=false)`，得到 `task_id=entv2_1780304230109079297`。本轮里 `ping` 在 `t+20=16:57:41` 先返回 `running / last_updated=2026-06-01T16:57:30+08:00`，`snmp` 则到 `t+80=16:58:42` 才返回 `running / last_updated=2026-06-01T16:58:40+08:00`；设备级 `GET /api/v1/device/v2/DVCE1EE3F7D394C` 也同步显示 `runtime_health=healthy / last_updated=2026-06-01T16:58:40+08:00`。这说明 later input 的时间戳现在已经能真实反映它自己完成采集的时刻，不再被整轮更早时间抹平。 | passed-real |
| REAL-075 | agent runtime / burst replace / priority carry-over | 路由器 | real-agent-runtime-priority-carry-over-fix | 沿着“无限缩短 `snmp` 首轮时间”继续追时，又补出了另一层 burst apply 细节：如果 `snmp` 在较早一轮 `ReplaceAllInputs()` 中已经被更新，但后面又陆续来了 processor、output 或别的 input 任务，后续 replace 会再次清空 `inputStatus`；若 priority 只看“最后一轮新增/更新了谁”，较早那批已经更新过的 input 就会重新掉回完整 sweep。先在 `ctrlhub/controller/agent/pkg/ops/metrics` 补红灯测试 `TestReplaceAllInputs_KeepsEarlierUpdatedInputPrioritizedAcrossLaterUnrelatedReplace`，再让 collector 维护“当前 generation 里尚未完成首轮采集的 pending priority inputs”，后续无关 replace 会把这批名字一起带到最新 generation，而不是只保留最后一轮变化。定向回归 `go test ./pkg/ops/metrics -count=1` 通过。随后重启现场 agent 并再次对 `DVCE1EE3F7D394C` 执行真实 `store/start(async=false)`，得到 `task_id=entv2_1780305330979015003`；agent 日志确认本轮下发顺序真实是 `snmp -> processor -> output -> ping`。运行态观测中，`t0=17:15:53` 时 `ping` 已返回 `running / last_updated=2026-06-01T17:15:51+08:00`，而 `snmp` 到 `t+80=17:17:13` 才返回 `running / last_updated=2026-06-01T17:16:59+08:00`，设备级 `runtime_health` 也在同一时刻恢复为 `healthy`。这说明“较早更新的 input 会被后续无关 replace 挤掉优先级”这层 bug 已被测试和代码压住，但当前现场 `snmp` 窗口并没有比 `REAL-074` 再明显缩短，剩余瓶颈更像是 `snmp` 自身这轮 gather 的真实耗时，而不是 priority 丢失。 | passed-real |
| REAL-076 | agent runtime / priority backlog preemption | 路由器 | real-agent-runtime-priority-backlog-preemption-fix | 在给 agent 打开 debug 时长日志继续追 `REAL-075` 后，现场又补出一层更具体的根因：`snmp` 这条任务本身并不慢，真实 `Gathered metrics from input` 只花了 `3.85s`，但在重启后立即回放的场景里，它直到 `2026-06-01 17:23:46 +08:00` 才真正开始这一轮 gather，距离 `ApplyTask` 的 `17:22:35` 已过去约 `71s`。调试日志进一步显示，问题并不是 `snmp` 插件内部，而是之前恢复出来的一大批旧 priority backlog 仍排在前面，且由于我们把 pending priority 做成了无序集合，后续 replay 的新变更会和旧 backlog 一起按名字顺序重排，导致目标设备自己的 `ping/snmp` 仍要等几十秒。先补红灯测试 `TestReplaceAllInputs_PrioritizesCurrentBurstAheadOfOlderPendingBacklog`，再让 collector 在保留 pending set 的同时维护执行顺序：当前 burst 里新变更的 input 放在前面，旧 backlog 放后面，而不是整批重新按字典序排。定向回归 `go test ./pkg/ops/metrics -count=1` 通过；随后带着 debug 配置重启现场 agent，并在“恢复 41 条持久化任务后、老 backlog 还没扫完”的最差场景下再次对 `DVCE1EE3F7D394C` 执行真实 `store/start(async=false)`，得到 `task_id=entv2_1780306024767573333`。这次日志里 `snmp` 于 `17:27:17.262` 收到 apply，`ping` 在 `17:27:19.868` 以 `2.05s` 完成首轮 gather，`snmp` 在 `17:27:23.830` 以 `3.96s` 完成首轮 gather；对应 runtime 也在 `last_updated=17:27:19` 与 `17:27:23` 收敛为 `running`。这说明“旧 priority backlog 抢在当前 replay 前面”这一层已经被真实压掉，`snmp` 首轮窗口已从之前的约 `80s` 收敛到接近 `ping + snmp` 自身耗时的 `6~7s`。 | passed-real |
| REAL-077 | agent runtime / telegraf-style input runner | 路由器 | real-agent-runtime-per-input-runner-refactor-validated | 按 [2026-06-01-agent-telegraf-style-input-scheduler.md](/home/jacky/project/OneOPS-ALL/docs/superpowers/plans/2026-06-01-agent-telegraf-style-input-scheduler.md) 完成 agent 侧 Telegraf 式重构后，再次在 `2026-06-01 18:28 +08:00` 的真实现场验证最差场景：重启 `agent.debug.yaml` 后，`TelegrafTaskManager` 先恢复了 `41` 条持久化任务，再对 `DVCE1EE3F7D394C` 执行真实 `store/start(async=false)`，得到 `task_id=entv2_1780309688085335105`。本轮 `store/start` 响应返回时，两条单条 `/runtime` 已直接在当前版本 `1780309699` 上收敛为 `running`，其中 `ping.last_updated=2026-06-01T18:28:19+08:00`、`snmp.last_updated=2026-06-01T18:28:19+08:00`；到 `t+20=18:28:40` 再查时，`device_v2 detail` 已显示 `runtime_health=healthy / last_updated=2026-06-01T18:28:21+08:00 / 2 个监控任务正在 agent 正常运行`，`ping` 继续推进到 `last_updated=18:28:21`，`snmp` 则稳定保持 `18:28:19`。与此同时，agent debug 日志已能在 `18:28:23.xxx` 连续看到 `172.32.2.15` 这条 `snmp-passthrough` 的真实 metric dump，说明当前实现已经不再依赖“全局串行 sweep + priority backlog 修补”才能勉强追平，而是以每个 input 独立 runner 的方式，在恢复了旧任务 backlog 的情况下也能让目标设备本轮 `ping/snmp` 直接完成 fresh gather。 | passed-real |
| REAL-078 | agent runtime / local replay revalidation | 路由器 | real-agent-runtime-local-replay-revalidation | 在继续补完 generic limiter 的取消路径、公平性与 `collection_offset` 修复后，又用当前工作树重新做了一轮本地真实复验。`2026-06-01 21:07 +08:00` 重启 `agent.debug.yaml` 后，agent 再次恢复了 `41` 条持久化任务；随后对 `DVCE1EE3F7D394C` 执行真实 `store/start(async=false)`，得到 `task_id=entv2_1780319299724584026`，任务版本推进到 `1780319314`。agent debug 日志显示 `21:08:34.311` 收到 `collect_agent-001_snmp-passthrough_172_32_2_15_161` 的 fresh apply，`21:08:34.421` 收到 `collect_agent-001_ping-basic_172_32_2_15_161` 的 fresh apply；随后 `21:08:34.524` 已出现 `172.32.2.15 / R10` 的 `snmp` metric dump，`21:08:36.486` 出现同目标的 `ping` metric dump，`21:08:37.728+` 继续出现 `snmp_interface` 明细指标。对应 API 侧在 `21:08:44` 查询时，`ping` 与 `snmp` 已都在当前版本 `1780319314` 上返回 `running`，其中 `ping.last_updated=2026-06-01T21:08:36+08:00`、`snmp.last_updated=2026-06-01T21:08:34+08:00`；设备级 `GET /api/v1/device/v2/DVCE1EE3F7D394C` 同步显示 `runtime_health=healthy / last_updated=2026-06-01T21:08:36+08:00 / 2 个监控任务正在 agent 正常运行`。到 `t+~56s=21:09:30` 再查时，两条 runtime 仍保持 `running / version=1780319314`，设备级健康也保持 `healthy`；由于当前 `telegraf_inputs.interval=60s`，这一时刻 `last_updated` 尚未再次推进属于预期，而不是回归。整体说明这批 follow-up 修复没有破坏真实采集链路，当前代码仍能在“恢复 41 条旧任务后本地 replay”的场景下稳定完成 fresh gather。 | passed-real |
